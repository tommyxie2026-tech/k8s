---
# 0066-kubevirt-smoke-test.yml
# Optional KubeVirt smoke test.
# Disabled by default. Creates an ephemeral containerDisk VM only when explicitly enabled.

- name: kubevirt smoke test
  hosts: localhost
  gather_facts: no
  roles:
    - kubevirt_smoke_test
